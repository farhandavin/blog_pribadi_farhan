<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
    <title>Blog Farhan Davin</title>
</head>
<body>

<h1>Blog Farhan Davin</h1>

<main class="container">

    <form action="/submit" method="POST">
        <label>
            <input type="text" name="judul" required placeholder="Masukkan judul">
        </label>
        <label>
            <textarea name="pesan" required placeholder="Tulis sebuah pesan...."></textarea>
        </label>
        
        <button type="submit" class="btn-submit">Submit</button>
    </form>

    <div class="blog">
        <% if(locals.blog && locals.blog.length > 0) { %>
            <h2>Blog Posts</h2>
            
            <ul class="blog-list">
                <% locals.blog.forEach(function(post, index) { %>
                    <li class="post-item">
                        <h3><%= post.judul %></h3>
                        <p><%= post.pesan %></p>

                        <div class="post-actions">
                            <a href="/edit/<%= index %>" class="btn-link">Edit</a>
                            
                            <form action="/delete" method="post" style="display: inline;">
                                <input type="hidden" name="postIndex" value="<%= index %>">
                                <button type="submit" class="btn-delete">Hapus</button>
                            </form>
                        </div>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p style="text-align: center;">Belum ada postingan.</p>
        <% } %>
    </div>

</main> <%- include("partials/footer.ejs") %>
</body>
</html>